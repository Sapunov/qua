upstream backend {
    server localhost:9000;
}

server {
    server_name 185.12.28.24;
    listen 80;

    location /static/ {
        root /var/www/qua/data;
    }


    location / {
        include uwsgi_params;
        uwsgi_pass backend;
    }

}
